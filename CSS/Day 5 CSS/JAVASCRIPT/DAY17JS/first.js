// Js is a synchronous single threaded language.
// Hoisting in JavaScript is the behavior where variable and function declarations are moved to the top of their containing scope during the compilation phase, before the code has been executed.] I
// This means that JavaScript "hoists" or lifts the declarations (b'** not the assignments) to the top, so you can refer to them h they are written in the code. However, only the declarat* hoisted, not the assignments or initializations.
// In other words; a variable can be used before it has been declared